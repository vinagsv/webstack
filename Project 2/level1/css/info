body {
  width: 100%;
  height: 100vh;
  margin: 0px;
  padding: 50px;
  padding-bottom: 30px;
  box-sizing: border-box;
}

.page-header {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.project-title {
  font-size: 40px;
  text-transform: uppercase;
  font-weight: 700;
  font-family: "Poppins";
}

/* Carousel style starts */
/* To align carousel at center of the available space of viewport */
/* Here we are applying height to img tag and parent elements are adaptive to that, that will help over layout to adot with changes in object fit property */

.carousel-wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.carousel-slides-container {
  width: fit-content;
  position: relative;
}

.carousel-slide {
  height: calc(100vh - 20vh);
  width: 100%;
  border-radius: 6px;
  object-fit: contain;
}

.prev-btn,
.next-btn {
  position: absolute;
  top: 50%;
  /*transform: translateY(-50%);*/ /*later*/
  width: auto;
  padding: 16px;
  font-size: 20px;
  color: white;
  /* border: none; */
  cursor: pointer;
  background: rgba(255, 255, 255, 0.3);
}

.prev-btn {
  left: 16px;
}

.next-btn {
  right: 16px;
}

/* .carousel-slide {
  display: none;
} */

.carousel-dots {
  text-align: center;
  margin-top: 20px;
}

.dot {
  height: 15px;
  width: 15px;
  margin: 0px, 5px;
  background-color: #bbb;
  border-radius: 50%;
  display: inline-block;
  /* transition: background-color 0.6s ease; */
  cursor: pointer;
}

.dot:hover {
  background-color: #ce036a;
}
.dot-active {
  background-color: #ce036a;
}

/* Carousel style end */

.copyright-text {
  width: 100%;
  text-align: center;
  font-family: "Poppins";
  margin: 20px 0 0 0;
}
